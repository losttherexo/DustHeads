function NewFindCard({record, description}){
    console.log(record)

    return (
        <div className='border flex flex-col my-4 h-40'>
            {/* <img src={record.image} alt={record.title} cl/> */}
        </div>
    )
}

export default NewFindCard